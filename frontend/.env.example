# API Configuration
# URL of the backend API - will be overridden in Docker to use service name
NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment Configuration
NODE_ENV=development

# Development Settings
# Enable file watching in Docker (needed for some systems)
WATCHPACK_POLLING=true 